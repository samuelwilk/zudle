{#    <nav class="bg-white shadow-md"> #}
{#        <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8"> #}
{#            <div class="flex justify-between h-16"> #}
{#                <div> #}
{#                    <!-- Logo --> #}
{#                    <img src="{{ asset('images/zu_logo_circle.png') }}" alt="Logo" class="max-h-full"> #}
{#                </div> #}

{#                <!-- Authentication Links --> #}
{#                <div class="space-x-8 sm:flex sm:items-center sm:ml-6"> #}
{#                    {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %} #}
{#                        <a href="{{ path('app_user') }}" class="text-sm text-gray-700 underline">Profile</a> #}
{#                    {% else %} #}
{#                        <a href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}" class="text-sm text-gray-700 underline"> #}
{#                            Login #}
{#                        </a> #}
{#                    {% endif %} #}
{#                </div> #}
{#            </div> #}
{#        </div> #}
{#    </nav> #}

<nav class="bg-zu-white border-zu-grey dark:bg-zu-black">
    <div class="max-w-screen-xl flex flex-wrap items-center justify-between mx-auto p-4">
        <a href="{{ path('app_dashboard') }}" class="flex items-center space-x-3 rtl:space-x-reverse">
            <span class="self-center text-2xl font-semibold whitespace-nowrap text-zu-red">zudle</span>
        </a>
        <button data-collapse-toggle="navbar-default" type="button"
                class="inline-flex items-center p-2 w-10 h-10 justify-center text-sm text-gray-500 rounded-lg md:hidden focus:outline-none focus:ring-2 focus:ring-gray-200 dark:text-gray-400 dark:focus:ring-gray-600"
                aria-controls="navbar-default" aria-expanded="false">
            <span class="sr-only">Open main menu</span>
            <svg class="w-5 h-5" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 17 14">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                      d="M1 1h15M1 7h15M1 13h15"></path>
            </svg>
        </button>
        <div class="hidden w-full md:block md:w-auto" id="navbar-default">
            <ul class="font-medium flex flex-col p-4 md:p-0 mt-4 border border-zu-white dark:border-zu-black rounded-lg bg-zu-white dark:bg-zu-black md:flex-row md:space-x-8 rtl:space-x-reverse md:mt-0">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    <li>
                        <a href="{{ path('app_dashboard') }}"
                           class="block py-2 px-3 text-zu-lighter-black rounded hover:bg-zu-red md:hover:bg-transparent md:border-0 md:hover:text-zu-red md:p-0 dark:text-zu-white md:dark:hover:text-zu-red dark:hover:bg-zu-lighter-black dark:hover:text-white md:dark:hover:bg-transparent"
                           aria-current="page">Home</a>
                    </li>
                    <li>
                        <a href="{{ path('app_stats') }}"
                           class="block py-2 px-3 text-zu-lighter-black rounded hover:bg-zu-red md:hover:bg-transparent md:border-0 md:hover:text-zu-red md:p-0 dark:text-zu-white md:dark:hover:text-zu-red dark:hover:bg-zu-lighter-black dark:hover:text-white md:dark:hover:bg-transparent">Stats</a>
                    </li>
                {% else %}
                    <li>
                        <a href="{{ path('hwi_oauth_service_redirect', {'service': 'google' }) }}"
                           class="block py-2 px-3 text-zu-lighter-black rounded hover:bg-zu-red md:hover:bg-transparent md:border-0 md:hover:text-zu-red md:p-0 dark:text-zu-white md:dark:hover:text-zu-red dark:hover:bg-zu-lighter-black dark:hover:text-white md:dark:hover:bg-transparent">
                            Login
                        </a>
                    </li>
                {% endif %}
{#                TODO: keep one for admin?#}
{#                <li>#}
{#                    <a href="#"#}
{#                       class="block py-2 px-3 text-gray-900 rounded hover:bg-zu-red md:hover:bg-transparent md:border-0 md:hover:text-blue-700 md:p-0 dark:text-white md:dark:hover:text-blue-500 dark:hover:bg-gray-700 dark:hover:text-white md:dark:hover:bg-transparent">Services</a>#}
{#                </li>#}
            </ul>
        </div>
    </div>
</nav>

